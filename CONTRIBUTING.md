<div dir="rtl">

# دليل المساهمة

شكراً لاهتمامك بتحسين المشروع. هذا الدليل يختصر عليك الوقت ويجعل مراجعة مساهمتك أسرع.

---

## قبل أن تبدأ

### القاعدة الأولى: قِس قبل أن تقترح

هذا المشروع **يقيس نفسه بصراحة**. الاختبار التاريخي ([`backtest-report.md`](backtest-report.md))
يُظهر أن الاستراتيجيات الحالية **لا تملك حافة قابلة للتعميم خارج العينة**.

لذلك أي مساهمة تعد بتحسين الإشارات يجب أن تُرفق بقياس:

```bash
python backtest.py --compare-variants --days 120 --oos-days 120 \
  --symbols BTC,ETH,SOL,BNB,XRP,DOGE,ADA,AVAX,LINK,SUI \
  --oos-symbols ARB,SEI,TIA,PYTH,JUP,WIF,PEPE,FET,RUNE,AAVE
```

مساهمة تُحسّن داخل العينة فقط **لن تُقبل** — هذا هو الدرس الأساسي الذي وثّقه
التقرير: إعدادات بدت رابحة (+0.366R) انقلبت سالبة (−0.108R) على عينة مستقلة.

### ما نرحّب به

- إصلاح أخطاء منطقية أو حسابية مع اختبار يثبتها
- تحسين الصلابة (معالجة أخطاء، نهايات بديلة، مهلات)
- اختبارات جديدة ترفع التغطية
- تحسينات وصولية (a11y) وأداء في الواجهة
- توثيق أوضح
- استراتيجيات جديدة **مقاسة داخل العينة وخارجها**

### ما لا نرحّب به

- رفع حدود العتبات لجعل الإشارات "تبدو أكثر" دون قياس
- إضافة مفاتيح API أو أي كود يرسل أوامر تداول
- اعتماد خارجي في الواجهة (CDN، خطوط، مكتبات JS)
- commit لبيانات مولَّدة أو أسرار

---

## بيئة التطوير

```bash
git clone https://github.com/turky4500/crypto-signals-arabic.git
cd crypto-signals-arabic

python3 -m venv .venv
source .venv/bin/activate          # ويندوز: .venv\Scripts\activate
pip install -r requirements-dev.txt

pytest                             # يجب أن تنجح كل الاختبارات
ruff check . && ruff format --check .
```

**Python المدعوم:** 3.11 و3.12 و3.13 (CI يختبر الثلاثة).

### إذا كانت Binance محجوبة في منطقتك

ترجع Binance **HTTP 451** لبعض المناطق. الماسح يجرّب النهايات تلقائياً، لكن للتثبيت:

```bash
export BINANCE_ENDPOINT=https://data-api.binance.vision
```

---

## سير العمل

1. **اعمل على فرع** من `main`:

   ```bash
   git checkout -b fix/rsi-warmup
   ```

2. **شغّل الفحوص محلياً قبل الدفع** — CI سيرفض التنسيق غير المطابق:

   ```bash
   ruff format .
   ruff check . --fix
   pytest
   ```

3. **التزم برسائل commit واضحة.** المستودع يستخدم العربية في الرسائل:

   ```
   إصلاح فترة إحماء RSI لتبقى NaN بدل 100

   كان fillna(100) يُنتج قيماً خاطئة في أول 14 شمعة، وقد تُمرَّر إلى
   التحليل إن كان الإطار قصيراً. أُضيف اختبار يغطي الحالة.
   ```

4. **افتح Pull Request** واذكر فيه:
   - المشكلة التي تحلّها (أو رقم Issue)
   - كيف اختبرتها
   - إن غيّرت منطق الإشارات: **أرقام الاختبار التاريخي داخل العينة وخارجها**

---

## قواعد الكود

### Python

- **تنسيق `ruff format`** إلزامي (طول السطر 110). لا تعدّل التنسيق يدوياً.
- **Type hints** لكل الدوال العامة، مع `from __future__ import annotations`.
- **Docstrings بالعربية** توضّح *لماذا* لا *ماذا* فقط.
- **لا `.iloc` كتابة داخل حلقات** — استخدم مصفوفات numpy خام
  (انظر `supertrend_direction` كمثال على السبب).
- **`SettingWithCopyWarning` مُحوَّل إلى خطأ** في `pyproject.toml`. إن ظهر،
  فالسبب غالباً أنك مرّرت slice إلى دالة تكتب أعمدة — انسخ الإطار صراحةً.
- **لا منطق موازٍ:** حسابات المؤشرات تعيش في `enrich()` وحدها. الإنتاج
  والاختبار التاريخي يقرآن نفس الأقنعة. لا تستنسخ شرطاً استراتيجياً في مكان ثانٍ.

### الاختبارات

- كل مؤشر مالي يحتاج **مرجعاً مستقلاً** مكتوباً بالحلقة الصريحة، لا مقارنة
  التنفيذ بنفسه.
- البيانات **صناعية وقابلة للتكرار** (`np.random.default_rng(seed)`) —
  ممنوع الاتصال بالشبكة في الاختبارات.
- غطّ الحالات الحدية: سوق ثابت، صعود محض، هبوط محض، إطار قصير، قيم `NaN`.

### الواجهة

- **لا طلبات خارجية حاجبة.** CI يفشل إن وُجد `src`/`href` خارجي غير روابط
  يفتحها المستخدم بنفسه.
- CSS وJS مضمّنان في `index.html` عمداً — ملف واحد يعمل دون شبكة.
- احترم `prefers-reduced-motion` و`dir="rtl"` ووصولية لوحة المفاتيح.
- ميّز دائماً بين **"لا بيانات"** و**"فشل تحميل"** — خلطهما كان خطأً مُصلحاً.

---

## إضافة استراتيجية جديدة

1. أضف الكود إلى `STRATEGY_NAMES` (أو `EXIT_NAMES`).
2. أضف القناع في `enrich()` — عمود `sig_<CODE>` منطقي.
3. أضف الكود إلى `ENTRY_CODES` أو `EXIT_CODES`.
4. أضف صياغة السبب في `_entry_reason()` أو `_exit_reason()`.
5. أضف اسم الاستراتيجية إلى أزرار التصفية — الواجهة تبنيها تلقائياً من
   `strategy_names`/`exit_names` في البيانات.
6. **شغّل الاختبار التاريخي داخل العينة وخارجها** وأرفق النتائج.
7. حدّث جدول الاستراتيجيات في `README.md`.

> لا تلمس `index.html` لتعريف الاستراتيجية — الأعمدة والشارات تُبنى من البيانات.

---

## الأمان

- **لا ترفع أي سر إطلاقاً.** لا توكن GitHub، ولا مفتاح Binance، ولا webhook.
- `.gitignore` يستبعد `.env` و`*.key` و`credentials.json` — لا تتجاوزه بـ `git add -f`.
- الأسرار في CI تُوضع في **Settings → Secrets and variables → Actions** فقط.
- إن ارتكبت سرّاً بالخطأ: **احذفه من مصدره فوراً** (إلغاء التوكن)، لا تكتفِ
  بحذف الـ commit — يبقى في التاريخ.
- للإبلاغ عن ثغرة: راجع [`SECURITY.md`](SECURITY.md).

---

## الأسئلة

افتح [Issue](https://github.com/turky4500/crypto-signals-arabic/issues) — نفضّل
سؤالاً مبكراً على طلب سحب كبير يُرفض.

شكراً لمساهمتك 🌱

</div>
