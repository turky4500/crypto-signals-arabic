<div dir="rtl">

# سجل التغييرات

التنسيق مبني على [Keep a Changelog](https://keepachangelog.com/ar/1.1.0/)،
والمشروع يلتزم [بالترقيم الدلالي](https://semver.org/lang/ar/).

## [2.0.0] — 2026-09-06

إصدار إعادة بناء شامل بعد تدقيق كامل ([`docs/audit-2026-09-06.md`](docs/audit-2026-09-06.md)).
رقم الإصدار الرئيسي رُفع لأن **مخطط `signals.json` تغيّر بشكل غير متوافق** ولأن
معنى الإشارات نفسه تغيّر جوهرياً.

### مُصلَح — صحة الإشارات

- **الشمعة الجارية لم تعد تُحلَّل.** كان الماسح يحسب كل المؤشرات على شمعة عمرها
  5 دقائق من أصل 60، فتظهر الإشارات وتختفي داخل الساعة (Repainting). قِيس الأثر:
  **20 زوجاً** كانت تختلف بين الشمعة الجارية والمغلقة. الآن تُستبعد الشمعة الجارية
  دائماً، ويُرصد ذلك في الحقل `candle_state: "closed"`.
- **المسح مثبّت على ساعة محددة** عبر `endTime`، فكل الأزواج في الدورة الواحدة
  يستخدمون نفس الشمعة المرجعية. سابقاً كان المسح الذي يعبر حدّ الساعة يُنتج
  شموعاً مختلطة (19:00 و20:00 معاً) في نفس الملف.
- **النتيجة قابلة لإعادة الإنتاج بايت-ببايت** عند تثبيت `--as-of` (تحقّق مُختبَر).
- **`volume_ratio` لم يعد مشوّهاً.** كان يُحسب على شمعة ناقصة فينتج ~0.1x دائماً،
  مما جعل ترتيب الجدول بأكمله ضجيجاً.
- **استُبعدت الأزواج المستقرة** (`USDC`, `FDUSD`, `TUSD`, `EUR`, `USD1`, `BFUSD`,
  `USDE`, `XUSD` وغيرها) — كانت 7 أزواج على الأقل تُفحص بلا معنى.
- **لا تكرار للزوج الواحد.** كان الزوج الذي يحقق استراتيجيتين يظهر صفّين منفصلين
  ويضخّم العدّاد. الآن سجل واحد بحقل `strategies: [...]`.
- **الاستراتيجية B أُحييت.** كانت تُنتج **صفر إشارات دائماً** لأنها تشترط تقاطعاً
  في ساعة واحدة *و* `vol_ratio > 1.5` على شمعة قيمتها الحقيقية ~0.1. الآن نافذة
  التقاطع 3 شموع.
- **شُدّدت الاستراتيجية A** بترتيب المتوسطات السليم وتغيّر 24س ≥ +1% وحجم ≥ 1.2×.
  معدل الإصابة انخفض من **60.7%** (105 من 173) إلى نحو **4%** — من وصف للسوق إلى فرز فعلي.
- **RSI للسوق الثابت أصبح 50 لا 100.** الحالة السابقة `avg_gain == avg_loss == 0`
  كانت تُرجع 100 خطأً.
- **فترة إحماء RSI تبقى `NaN`** بدل `fillna(100)` التي كانت تُنتج قيماً خاطئة.
- **رُفع `KLINE_LIMIT` من 250 إلى 400.** الاختبارات تُظهر أن RSI يبقى منحرفاً
  حتى 5.7 نقطة عن صيغة Wilder الكتابية عند الشمعة 20، ويتطابق تماماً بعد الشمعة 250.
- **`SettingWithCopyWarning` اختفى** بنسخ الإطار صراحةً داخل الدوال.

### مُضاف — إدارة المخاطر

- **`stop_loss` و`take_profit` لكل إشارة**، مشتقان من ATR (2.5× و5×) مع `risk_reward`.
  الإصدار الأول كان يعرض "إشارة شراء" بلا وقف ولا هدف.
- **نتيجة مرجّحة `score` من 0 إلى 100** (سيولة 25 + زخم 20 + جودة RSI 15 +
  تأكيدات 25 + ترتيب المتوسطات 15) بدل الترتيب على `volume_ratio` وحده.
- **`tier`** يميّز الطبقة الأولى (تتضمن الاستراتيجية A) المدعومة بالاختبار التاريخي.
- **تحذيرات الخروج** (`X1`–`X4`): كسر الاتجاه، التشبع الشرائي، تقاطع MACD الهابط،
  فقدان EMA200. كانت الإشارات أحادية الاتجاه تماماً.
- **الاستراتيجية D الجديدة:** الارتداد من EMA20 داخل اتجاه صاعد.
- **`change_24h`** و**`spark`** (آخر 24 إغلاقاً) للرسم المصغّر في الواجهة.
- **عمر الإشارة:** `first_seen` و`age_hours` و`is_new`، تُحسب بقراءة النسخة
  المنشورة السابقة — بلا تخزين حالة في المستودع.

### مُضاف — الاختبار التاريخي

- **`backtest.py` جديد بالكامل:** محاكاة مشي-للأمام على بيانات Binance الفعلية،
  بدخول عند افتتاح الشمعة التالية (لا lookahead)، وفحص الوقف/الهدف داخل الشموع،
  ورسوم 10 نقطة أساس لكل طرف وانزلاق 5 نقاط أساس، ومركز واحد لكل زوج.
- **`--compare-variants` و`--oos-days` و`--oos-symbols`** لقياس الاتساق **خارج العينة**.
- **يستخدم نفس دوال الإنتاج حرفياً** (`enrich` و`Config`) فلا يوجد منطق موازٍ قد ينحرف.
- **`backtest-report.md` مُلتزَم** ويوثّق بصراحة أن **أي إعداد لم يحقق توقعاً إيجابياً
  خارج العينة**. هذه أهم إضافة في الإصدار: المشروع أصبح يقيس نفسه بدل أن يفترض.

### مُضاف — الصلابة

- **قائمة نهايات Binance مع تبديل تلقائي.** ترجع Binance **HTTP 451** (حظر جغرافي)
  في بعض المناطق، وكانت نهاية واحدة ثابتة تعني فشلاً كاملاً. الآن:
  `api.binance.com → data-api.binance.vision → api1 → api2 → api3`.
- **حارس ضد البيانات الفاسدة:** إذا فشل أكثر من 50% من الأزواج يخرج الماسح برمز 3
  **ولا يكتب** `signals.json`، فتبقى النسخة الصالحة منشورة. سابقاً كان يُكتب
  `{"scanned": 0, "signals": []}` ويمسح بيانات جيدة.
- **`supertrend_direction` يعمل على مصفوفات numpy خام** بدل `.iloc` — أسرع بعشرات
  المرات (~43,000 كتابة `.iloc` كانت تُهدر كل دورة).
- **`enrich()` مُوجَّه بالكامل** يحسب المؤشرات وأقنعة الإشارات على الإطار كاملاً
  مرة واحدة، وهو مصدر الحقيقة الوحيد للإنتاج والاختبار التاريخي معاً.

### مُضاف — الواجهة

- **صفر اعتماد خارجي:** حُذف `cdn.tailwindcss.com` (المصرّح أنه للتطوير فقط،
  ~400KB تُبنى في المتصفح) وGoogle Fonts. كل CSS محلي والخطوط من النظام.
- **العدّاد التنازلي لم يعد مزيّفاً.** كان يُحسب من لحظة فتح الصفحة؛ الآن يُشتق
  من `as_of` الحقيقي، مع تمييز ثلاث حالات: يعمل / متأخر / قديم.
- **فصل حالات "لا إشارات" و"فشل التحميل"** — سابقاً كانتا رسالة واحدة مضلِّلة.
  الآن حالة خطأ مستقلة برسالة التشخيص وزر إعادة محاولة.
- **تبويبان** لإشارات الدخول وتحذيرات الخروج.
- **جدول قابل للترتيب** بالنقر على العناوين، مع قائمة ترتيب بديلة.
- **صف تفاصيل قابل للتوسيع** يعرض أسباب الإشارة وقيم الشمعة المغلقة
  (EMA20/50/200، ATR، حجم 24س، المخاطرة/العائد، أول ظهور).
- **رسم مصغّر (sparkline)** بـ SVG مضمّن لكل عملة.
- **أعمدة وقف الخسارة وهدف الربح** وتغيّر 24 ساعة والنتيجة والطبقة والعمر.
- **شريط صدق بارز** يلخّص نتيجة الاختبار التاريخي ويربط التقرير الكامل.
- **تحديث تلقائي كل 5 دقائق** بدل ساعة كاملة، مع cache-busting.
- **اختصار `/`** للقفز إلى البحث، ودعم لوحة المفاتيح للترتيب والتوسيع.
- **`favicon`** بـ SVG مضمّن كـ data URI (بلا ملف إضافي ولا طلب شبكة).
- **وسوم Open Graph وTwitter Card** و`theme-color` و`og.png`.

### مُضاف — CI/CD

- **سير عمل واحد `update.yml`** يجمع الفحص والنشر، بدل `scanner.yml` + `pages.yml`.
- **لا commit لـ `signals.json` إلى `main`.** الإصدار القديم كان يلتزم ~8,760 commit
  سنوياً، وكل واحد يشغّل نشراً. الآن تُنشَر البيانات ضمن artifact مباشرة.
- **`timeout-minutes: 20`** — سابقاً كان تشغيل عالق يستهلك حتى 6 ساعات من دقائق Actions.
- **`concurrency` بلا إلغاء** — يمنع تراكب دورتين وتعارض الدفع.
- **رُفعت إصدارات Actions** إلى `checkout@v7` و`setup-python@v7` و`configure-pages@v6`
  و`upload-pages-artifact@v5` و`deploy-pages@v5` (تحقّق من وجود الوسوم عبر API).
  يحذف تحذير إهمال Node.js 20.
- **`ci.yml` جديد:** `ruff format --check` و`ruff check` و`pytest` على Python
  3.11 و3.12 و3.13، إضافة إلى فحص سلامة الواجهة (توازن أقواس CSS، عدم وجود موارد
  خارجية حاجبة، اكتمال مفاتيح `signals.json`).
- **ملخص الدورة في GitHub Step Summary** بجدول أفضل 5 إشارات.
- **`requirements.txt` بإصدارات مثبّتة** (`==`) بدل `>=` — مع `cache: pip` كان
  البناء غير قابل لإعادة الإنتاج، وإصدار pandas جديد قد يكسر `ewm` بصمت.
- **تشغيل يدوي بحقول إدخال** (`as_of`, `top`, `min_confirmations`).

### مُضاف — ملفات المشروع

- **`LICENSE` (MIT).** بدونه كانت كل الحقوق محفوظة قانونياً ولا يجوز لأحد استخدام
  الكود أو تعديله — كان هذا أهم نقص في المستودع.
- **`.gitignore`** بقسم صريح للأسرار (`.env`, `*.key`, `credentials.json`).
- **`tests/` بـ 63 اختباراً** تقارن المؤشرات مقابل مراجع مستقلة مكتوبة بالحلقة الصريحة.
- **`pyproject.toml`** بإعداد ruff وpytest، مع تحويل `SettingWithCopyWarning` إلى خطأ.
- **`404.html`** مصمّم بنفس هوية اللوحة، مع إعادة توجيه للمسارات الخاطئة.
- **`CHANGELOG.md`** (هذا الملف) و**`CONTRIBUTING.md`** و**`SECURITY.md`**.
- **`.editorconfig`** و**`requirements-dev.txt`** و**`.env.example`**.
- **`docs/audit-2026-09-06.md`** — تقرير التدقيق الأصلي الذي بُني عليه هذا الإصدار.

### مُغيَّر — `README.md`

- استُبدلت تعليمات النشر النظرية (`USERNAME/REPOSITORY`) بالرابط الحي الفعلي.
- أُضيف قسم **"اقرأ هذا أولاً"** يعرض جدول نتائج الاختبار التاريخي بصراحة.
- وُثّق مخطط `signals.json` كاملاً بحقل حقل.
- شُرحت **مبررات كل عتبة** بدل سردها بلا تعليل.
- أُضيفت شارات CI والترخيص وإصدارات Python.
- أُضيفت نسخة إنجليزية مختصرة.
- ذُكر `pages.yml` الغائب سابقاً عن جدول الملفات (وأُعيدت هيكلة الجدول بالكامل).

### معلوم — قيود مقصودة

- **قائمة الأزواج المرشحة غير قابلة لإعادة الإنتاج تماماً**: تُشتق من ticker الـ24
  ساعة الحيّ، فقد يتغير العدد ب±1 بين تشغيلين حين يعبر زوجٌ عتبة المليون.
  قيد جوهري في البيانات العامة، وموثّق في README.
- **انحياز البقاء في الاختبار التاريخي**: قائمة الأزواج مأخوذة من اليوم لا من
  تاريخ بداية الفترة، والأزواج المشطوبة غير ممثّلة. موثّق في التقرير.

## [1.0.0] — 2026-09-06

- الإطلاق الأول: `scanner.py` بثلاث استراتيجيات (A/B/C)، لوحة عربية RTL
  بـ Tailwind CDN، ونشر عبر GitHub Actions.

[2.0.0]: https://github.com/turky4500/crypto-signals-arabic/releases/tag/v2.0.0
[1.0.0]: https://github.com/turky4500/crypto-signals-arabic/commit/52a3f2e

</div>
