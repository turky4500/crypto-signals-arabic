<div dir="rtl">

# سياسة الأمان

## الإبلاغ عن ثغرة

**لا تفتح Issue عاماً لأي ثغرة أمنية.**

أرسل التفاصيل عبر واحدة من:

- **GitHub Security Advisory (مفضّل):** مستودع → تبويب **Security** →
  **Report a vulnerability** → *Advisory* → **Report a vulnerability**
- أو تواصل مع مالك المستودع [@turky4500](https://github.com/turky4500) مباشرة.

سنؤكد الاستلام خلال **72 ساعة** قدر الإمكان، ونعمل على إصلاح الثغرات الحرجة
قبل أي إعلان عام.

### ما نحتاجه في البلاغ

- وصف الثغرة وخطوات إعادة الإنتاج
- الإصدار أو الـ commit المتأثر
- الأثر المتوقع (تسريب بيانات؟ تنفيذ كود؟ تضليل المستخدم؟)
- إن أمكن: إثبات مفهوم (PoC) محدود الأثر

---

## نطاق السياسة

### داخل النطاق

- `scanner.py` و`backtest.py` — أي مسار قد يسرّب بيانات أو يُنفَّذ بشكل غير مقصود
- `index.html` — XSS، حقن عبر بيانات `signals.json`، تحميل موارد خارجية غير متوقعة
- `.github/workflows/*` — تصعيد صلاحيات، تسريب أسرار إلى السجلات، تبعيات غير مثبّتة
- سلسلة التوريد: اعتماديات Python، إصدارات GitHub Actions

### خارج النطاق

- ثغرات في واجهة Binance نفسها (بلّغ Binance مباشرة)
- نتائج الإشارات أو دقة الاستراتيجيات — هذه **مسألة منهجية لا أمنية**،
  وموثّقة بشفافية في [`backtest-report.md`](backtest-report.md)
- هجمات تتطلب وصولاً فيزيائياً أو هندسة اجتماعية للمستخدم

---

## الموقف الأمني الحالي

### ما نلتزم به

- **لا مفاتيح API إطلاقاً.** المشروع يقرأ من واجهة Binance العامة فقط
  (`/api/v3/exchangeInfo`, `/ticker/24hr`, `/klines`). لا يوجد كود تداول،
  ولا توقيع طلبات، ولا اتصال بحساب أي مستخدم. **أسوأ حالة ممكنة هي بيانات
  سوق خاطئة — لا خسارة أموال.**
- **أقل الصلاحيات في CI.** سير عمل النشر يملك `contents: read` و`pages: write`
  و`id-token: write` فقط — ولا يملك صلاحية الكتابة على المستودع إطلاقاً
  (تحسين مقصود على الإصدار الأول الذي كان يملك `contents: write`).
- **لا commit آلي للكود.** الإصدار الأول كان يدفع `signals.json` إلى `main`
  كل ساعة، وهو مسار كتابة آلي إلى المستودع. أُلغي بالكامل.
- **إصدارات مثبّتة.** `requirements.txt` يستخدم `==` لا `>=`، وإصدارات Actions
  مثبّتة على وسوم رئيسية تحقّق من وجودها.
- **`.gitignore` يستبعد الأسرار** (`.env`, `*.key`, `*.pem`, `credentials.json`).
- **الأسرار اختيارية.** إن لم يُضبط `NOTIFY_WEBHOOK` لا تُرسل إشعارات —
  لا يوجد مسار إجباري يتطلّب سرّاً.
- **تهريب HTML في الواجهة.** كل قيمة من `signals.json` تُمرَّر عبر `esc()`
  قبل الإدراج في DOM، لأن بيانات Binance (أسماء الأزواج) مصدر خارجي.
- **لا اعتماد خارجي في الواجهة.** لا CDN ولا خطوط خارجية — يقلّص سطح الهجوم
  ويمنع تعطّل اللوحة إذا سقط طرف ثالث.

### مخاطر متبقية معروفة

| الخطر | التخفيف |
|---|---|
| **تسميم البيانات:** لو أرجعت Binance قيماً مشوّهة ستُنشر على اللوحة | حارس يفشل الدورة إذا لم ينجح ≥50% من الأزواج، فلا تُمسح نسخة صالحة |
| **`NOTIFY_WEBHOOK` سرّ حساس** يسمح بالنشر لقناة المستخدم | يُقرأ من GitHub Secrets فقط، ولا يُطبع في السجلات |
| **الحظر الجغرافي (HTTP 451)** قد يُفشل الدورة في بعض المناطق | قائمة نهايات مع تبديل تلقائي |
| **انحياز البقاء** في الاختبار التاريخي | موثّق صراحةً في التقرير — ليس ثغرة تنفيذية لكنه حد منهجي |

---

## للمستخدمين

- **لا يحتاج هذا المشروع أي مفتاح API.** إن طلب منك فرع أو نسخة منه مفتاحاً،
  فهي ليست من هذا المستودع — لا تعطها لأحد.
- **لا تتداول بأموال حقيقية اعتماداً على هذه الإشارات.** التقرير التاريخي
  لا يدعم ذلك.
- **تحقّق من الرابط:** النسخة الرسمية الوحيدة هي
  <https://turky4500.github.io/crypto-signals-arabic/>
- إن رأيت نسخة تطلب بيانات دخول Binance أو تَعِد بأرباح مضمونة، فهي احتيال
  ينتحل اسم المشروع — بلّغ عنها.

---

## الإفصاح المنسّق

نفضّل الإفصاح المنسّق. نطلب مهلة **90 يوماً** قبل النشر العام إن أمكن،
وننسب الفضل للمبلّغ في ملاحظات الإصدار ما لم يطلب عدم ذكره.

</div>
