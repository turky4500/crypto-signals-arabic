# إصدارات مثبّتة عمداً: المشروع يُشغَّل تلقائياً كل ساعة في CI، وأي إصدار جديد
# من pandas/numpy قد يغيّر سلوك ewm أو iloc ويُنتج إشارات خاطئة بصمت.
# هذه هي الإصدارات نفسها التي اختُبر بها الكود (63 اختباراً + اختبار تاريخي).
requests==2.33.0
pandas==2.2.3
numpy==2.3.5
